package com.cibertec.monkey.service;

import java.math.BigDecimal;
import java.util.List;
import com.cibertec.monkey.entity.ProductoStock;  // ← sin DTO
import com.cibertec.monkey.entity.VentaResumen;   // ← sin DTO

public interface DashboardService {
    Long contarVentasHoy();
    BigDecimal totalIngresosMes();
    BigDecimal totalGananciaMes();
    Long contarProductosStockBajo(int umbral);
    List<ProductoStock> listarProductosStockBajo(int umbral);  // ← sin DTO
    List<VentaResumen> ultimasVentas();                        // ← sin parámetro
}